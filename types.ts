export interface Product {
	id: string;
	name: string;
	description: string;
	price: number;
}

export interface User {
	id: string;
	username: string;
	password: string;
}

export interface Car {
	id: string;
	model: string;
	price: number;
}